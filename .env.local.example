# Copy to .env.local and fill in to switch the AI assistant on.
# Get a key at https://console.anthropic.com/
# Without this, the chat UI still works — it just tells visitors it's off.
ANTHROPIC_API_KEY=
